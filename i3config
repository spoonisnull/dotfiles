set $mod Mod1
set $mod4 Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Input Mono Medium 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# fibonacci layout
for_window [class=".*"] split toggle

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status
        tray_output primary
        output DP-1
        # hide workspace names
        workspace_buttons no
  colors {
          background #2d2d2d
          statusline #dedede
          separator #2d2d2d
       #  focused_workspace  #d64937 #d64937 #dedede
       #  active_workspace   #2d2d2d #5f676a #dedede
       #  inactive_workspace #2d2d2d #2d2d2d #888888
       #  urgent_workspace   #333333 #900000 #ffffff
  }
}

# gaps
set $gap 5
gaps inner $gap
gaps outer $gap

# colours
# class                 border  backgr. text    indicator child_border
client.focused          #d64937 #d64937 #dedede #d64937   #d64937
client.unfocused        #333333 #333333 #dedede #333333   #333333
client.focused_inactive #333333 #333333 #dedede #333333   #333333
client.urgent           #383a3b #383a3b #ee0000 #383a3b   #383a3b

new_window normal
hide_edge_borders vertical 
new_window 1pixel

# autorun
exec ~/.screenlayout/i3_four_monitors.sh
exec --no-startup-id nitrogen --restore
exec nm-applet
exec shutter --min_at_startup
exec /opt/urserver/urserver-start --no-manager --no-notify
exec redshift-gtk
exec dbus-launch dropbox stop && dbus-launch dropbox start
exec caffeine-indicator
exec mopidy

bindsym $mod+Return exec st
bindsym $mod+w kill

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod4+h move left
bindsym $mod4+j move down
bindsym $mod4+k move up
bindsym $mod4+l move right

# split in horizontal and vertical orientation
bindsym $mod4+- split h
bindsym $mod4+\ split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
# bindsym $mod+t layout tabbed
# bindsym $mod+s layout toggle split

# toggle tiling / floating
bindsym $mod4+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# switch to workspace
bindsym $mod+1 focus output DP-1-1
bindsym $mod+2 focus output DVI-D-0
bindsym $mod+3 focus output DP-1
bindsym $mod+4 focus output HDMI-0

# move focused container to workspace
bindsym $mod+Shift+1 move container to output DP-1-1
bindsym $mod+Shift+2 move container to output DVI-D-0
bindsym $mod+Shift+3 move container to output DP-1
bindsym $mod+Shift+4 move container to output HDMI-0

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize
bindsym $mod+r mode "resize"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# volume controls generic (media keys)
bindsym XF86AudioRaiseVolume exec amixer -q set Master 5dB+ unmute
bindsym XF86AudioLowerVolume exec amixer -q set Master 5dB- unmute
bindsym XF86AudioMute exec amixer -D pulse set Master 1+ toggle

# volume controls no media keys
bindsym $mod+Control+bracketright exec amixer -q set Master 5dB+ unmute
bindsym $mod+Control+bracketleft exec amixer -q set Master 5dB- unmute
bindsym $mod+Control+p exec amixer -D pulse set Master 1+ toggle

bindsym $mod+e exec "pcmanfm"
bindsym Print exec "shutter -s"
bindsym $mod+F1 exec "google-chrome"
bindsym $mod+F2 exec ""
bindsym $mod+F3 exec ""
bindsym $mod+F4 exec ""
bindsym $mod+F5 exec ""
bindsym $mod+F6 exec ""
bindsym $mod+F7 exec ""
bindsym $mod+F8 exec ""
bindsym $mod+F9 exec ""
bindsym $mod+F10 exec ""
bindsym $mod+F11 exec ""
bindsym $mod+F12 exec ""
bindsym $mod+F13 exec ""
bindsym $mod+F14 exec ""
bindsym $mod+F15 exec ""
bindsym $mod+F16 exec ""
bindsym $mod+F17 exec ""
bindsym $mod+F18 exec ""
bindsym $mod+F19 exec ""
bindsym $mod+F20 exec ""
bindsym $mod+F21 exec ""
bindsym $mod+F22 exec ""
bindsym $mod+F23 exec ""
